# Reference only: run `make env` to generate local random credentials.
# Never copy this file into production or supply Railway credentials here.
DJANGO_SETTINGS_MODULE=config.settings.development
SECRET_KEY=
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
ENABLE_ADMIN=true
POSTGRES_PASSWORD=
DATABASE_URL=
E2E_DATABASE_URL=
